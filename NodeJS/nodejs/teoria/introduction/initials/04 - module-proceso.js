// MODULO PROCESO

/*

En informática, un proceso es la instancia de un programa informático 
que se está ejecutando.


Node tiene un processobjeto global con métodos útiles e información 
sobre el proceso actual.

La process.envpropiedad es un objeto que almacena y controla 
información sobre el entorno en el que se está ejecutando actualmente el proceso.

EXAMPLE:

if (process.env.NODE_ENV === 'development'){
  console.log('Testing! Testing! Does everything work?');
}
  
*/
