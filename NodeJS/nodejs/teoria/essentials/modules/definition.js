// TEORIC MODULES:

/*
- El eventsmódulo proporciona EventEmitterobjetos que se utilizan
 para asignar funciones de escucha que se activan en eventos específicos.

- El buffermódulo se utiliza para manejar datos binarios. En app.js ,
Bufferse crea un objeto y luego se lo convierte en una cadena.

- El fsmódulo se utiliza para interactuar con el sistema de archivos
 del usuario. En app.js , se registra una declaración que verifica 
 que se proporcionó un archivo en una función de devolución de llamada.

- El timermódulo proporciona la setImmediate()función que se ejecuta 
inmediatamente después de que se completa la fase de sondeo actual. 
Tome nota de cuándo 'Welcome to Node.js'se registra en la terminal.
*/